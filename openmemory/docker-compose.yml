services:
  mem0_store:
    image: qdrant/qdrant
    container_name: om-store
    ports:
      - "6333:6333"
    volumes:
      - mem0_storage:/mem0/storage
  openmemory-mcp:
    image: mem0/openmemory-mcp
    container_name: om-mcp
    build: api/
    environment:
      - USER=${USER:-user}
    env_file:
      - api/.env
    depends_on:
      - mem0_store
    ports:
      - "8765:8765"
    # For live reload with local source, replace named volume with bind mount:
    #   volumes:
    #     - ./api:/usr/src/openmemory
    #   command: sh -c "uvicorn main:app --host 0.0.0.0 --port 8765 --reload --workers 4"
    command: >
      sh -c "uvicorn main:app --host 0.0.0.0 --port 8765 --workers 4"
  openmemory-ui:
    container_name: om-ui
    build:
      context: ui/
      dockerfile: Dockerfile
    image: mem0/openmemory-ui:latest
    ports:
      - "3000:3000"
    env_file:
      - ui/.env
    environment:
      - NEXT_PUBLIC_USER_ID=${USER:-user}
      - NEXT_LOCAL_API_URL=http://openmemory-mcp:8765

volumes:
  mem0_storage:
